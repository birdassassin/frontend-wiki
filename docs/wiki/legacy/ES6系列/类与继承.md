# 类与继承

> 🟡 **中级** — 适合有一定基础的开发者，掌握 ES6 类语法。

---

## 1. 类的基本语法

### 1.1 定义类

```javascript
class Person {
  // 构造函数
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }
  
  // 实例方法
  greet() {
    console.log(`Hello, my name is ${this.name}`);
  }
  
  // 获取器
  get info() {
    return `${this.name}, ${this.age} years old`;
  }
  
  // 设置器
  set info(value) {
    const [name, age] = value.split(', ');
    this.name = name;
    this.age = parseInt(age);
  }
  
  // 静态方法
  static create(name, age) {
    return new Person(name, age);
  }
}

// 使用类
const person = new Person('Alice', 25);
person.greet(); // Hello, my name is Alice
console.log(person.info); // Alice, 25 years old
person.info = 'Bob, 30';
console.log(person.name); // Bob

// 静态方法
const person2 = Person.create('Charlie', 35);
```

---

## 2. 继承

### 2.1 使用 extends

```javascript
class Student extends Person {
  constructor(name, age, grade) {
    // 调用父类构造函数
    super(name, age);
    this.grade = grade;
  }
  
  // 覆盖父类方法
  greet() {
    console.log(`Hello, I'm ${this.name}, grade ${this.grade}`);
  }
  
  // 新增方法
  study() {
    console.log(`${this.name} is studying`);
  }
}

const student = new Student('Alice', 20, 'A');
student.greet(); // Hello, I'm Alice, grade A
student.study(); // Alice is studying
console.log(student.info); // Alice, 20 years old
```

### 2.2 super 关键字

```javascript
class Parent {
  constructor(name) {
    this.name = name;
  }
  
  greet() {
    return `Hello from Parent: ${this.name}`;
  }
}

class Child extends Parent {
  constructor(name, age) {
    super(name);
    this.age = age;
  }
  
  greet() {
    // 调用父类方法
    const parentGreet = super.greet();
    return `${parentGreet}, Age: ${this.age}`;
  }
}

const child = new Child('Alice', 25);
console.log(child.greet()); // Hello from Parent: Alice, Age: 25
```

---

## 3. 类的特性

### 3.1 私有属性

```javascript
class Person {
  // 私有属性（# 前缀）
  #password;
  
  constructor(name, password) {
    this.name = name;
    this.#password = password;
  }
  
  // 私有方法
  #validatePassword(password) {
    return this.#password === password;
  }
  
  login(password) {
    return this.#validatePassword(password);
  }
}

const person = new Person('Alice', '123456');
console.log(person.#password); // ❌ 报错：私有属性
console.log(person.login('123456')); // ✅ true
```

### 3.2 静态属性

```javascript
class Counter {
  static count = 0;
  
  constructor() {
    Counter.count++;
  }
  
  static get total() {
    return Counter.count;
  }
}

const c1 = new Counter();
const c2 = new Counter();
console.log(Counter.total); // 2
```

### 3.3 实例属性简写

```javascript
class Person {
  // 实例属性可以直接写在类顶层
  name = 'Unknown';
  age = 18;
  
  constructor(name, age) {
    if (name) this.name = name;
    if (age) this.age = age;
  }
}
```

---

## 4. 练习题

1. **创建类**：
```javascript
// 创建一个 Car 类，包含品牌、型号、颜色属性和 drive 方法
```

2. **创建子类**：
```javascript
// 创建 ElectricCar 继承 Car，添加电池容量属性和 charge 方法
```

3. **实现私有属性**：
```javascript
// 创建一个 BankAccount 类，包含私有余额属性
// 提供 deposit、withdraw、getBalance 方法
```

---

**下一篇**：[Generator 函数](Generator函数.md)

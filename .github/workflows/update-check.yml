name: Weekly Update Check

on:
  schedule:
    # 每周一上午 10:00 (UTC) 运行
    - cron: '0 10 * * 1'
  workflow_dispatch:  # 允许手动触发

jobs:
  check-updates:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Run update checker
        run: node scripts/update-checker.js
      
      - name: Check if report changed
        id: check-report
        run: |
          if git diff --quiet scripts/update-report.md 2>/dev/null; then
            echo "has_updates=false" >> $GITHUB_OUTPUT
          else
            echo "has_updates=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Create issue if updates needed
        if: steps.check-report.outputs.has_updates == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('scripts/update-report.md', 'utf8');
            
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `📦 知识库更新检查 - ${new Date().toISOString().split('T')[0]}`,
              body: report,
              labels: ['update', 'automated']
            });
      
      - name: Commit report
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add scripts/update-report.md
          git commit -m "chore: update version check report" || exit 0
          git push
